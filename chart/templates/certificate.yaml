apiVersion: cert-manager.io/v1
kind: Certificate
metadata:
  name: checkers
spec:
  secretName: checkers-tls
  dnsNames:
    - {{ .Values.domain | quote }}
  issuerRef:
    name: letsencrypt
    kind: ClusterIssuer
