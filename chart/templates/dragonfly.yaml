apiVersion: dragonflydb.io/v1alpha1
kind: Dragonfly
metadata:
  name: checkers-redis
spec:
  replicas: 1
  args:
    - "--bind=::"
    - "--admin_bind=::"
  resources:
    requests:
      cpu: 50m
      memory: 128Mi
    limits:
      cpu: 500m
      memory: 512Mi
